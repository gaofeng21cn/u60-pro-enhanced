# 验证范围与已知风险

以下表格中的既有上游记录仅指开发过程中**一台国行 B28**、特定 SIM/上游与 AX88179 网卡。不能把模拟测试等同于跨设备验收。

本分支已在一台国行 B31（`BD_CNMU5250V1.0.0B31`）完成首次安装、增强屏幕显示、双击电源键双向切换、原厂管理页及增强功能页访问。B31 首装后代理服务未自动启动；使用者在设备 UI 手动配置并启用内置 Mihomo 后，已确认代理可用。网页和屏幕均已验证能列出实际叶子策略组节点，网页节点切换会校验成员并回读当前值。该结果只证明这一台设备上的手动配置路径；B31 的 USB 网卡下游、Wi-Fi 接力完整联网、待机、长时稳定性和完整回退仍未验收，不能套用下表 B28 结果。

| 项目 | 已有证据 | 尚未验证/限制 |
|---|---|---|
| 五页屏幕/主题/字号/图标 | 开发机实际显示与按键验收；主机回归覆盖首页至少 44 像素触控区、管理子页进入/返回、原动作参数保留、当前选项标记与确认内容，取消不执行；B31 实机屏幕缓冲与输入事件核对首页、状态展开、管理页进入/返回、确认后取消、空连接不可操作和组网最近记录提示 | 实体触摸手感、不同屏幕批次、长时间稳定性 |
| 电源单击/双击/长按 | 用户实机确认正常 | 原厂“重启”曾表现为关机，需长按再开；未解决，不承诺无人值守重启。信号驱动切回原厂时可恢复渲染，但背光回读为 0，不能据此证明实体双击会点亮原厂屏幕；增强界面恢复后的背光正常 |
| Wi-Fi 2.4G/5G | 两频段连接上网、频段开关修复、长休眠后5G恢复更快 | 无精确恢复秒数承诺；不同信道/地区/DFS/拥塞场景未穷尽 |
| Wi-Fi 接力 | B31 原生信道切换与恢复经热点/驱动双重回读通过；小屏实机验证扫描、选择网络、键盘字符页、短密码拒绝、失败返回后再扫描； 修复后2.4G上游＋双热点手机百度/Google通过；2.4G/5G上游设备及USB业务检查；驱动信道一致、Tailscale自动通信 | 5G上游完整手机验收仍待完成；本修复版真实长休眠、冷启动、不同加密协议/认证门户、上游长期离线、探测结果的不同网络覆盖、长期功耗未验证；DFS不支持 |
| 首页出口标题（v0.1.2） | 复现慢快状态覆盖，修复后24秒94个缓冲画面标题一致 | 不代表未来中继不会掉线；自动蜂窝回退仍保留 |
| 接力策略与互联网探测 | 主机回归覆盖 B31 按启用意图启动、关闭开机连接、禁止蜂窝回退的 IPv4/IPv6 OUTPUT/FORWARD 规则、部分应用失败、切换上游保留保护，以及绑定 STA 的 204/门户/不可达判定与频率限制 | 规则保护不等于整机零流量；原厂开机阶段、硬件加速旁路、完整冷启动及真实断线保护仍须实机验收。检测失败不会自动改变路由 |
| USB 手动 LAN | B28 指定网卡的下游电脑百度/Google通过；B31 的按需服务启动、运行回读和未接网卡状态单独验证 | 仅验证指定AX88179；其他网卡不保证 |
| USB 数据线直连（Mac） | B31 重启后原厂 ConfigFS 组合（RNDIS、`ffs.adb` 等）可重新枚举，ADB 在线；源码已停用在线 USB 组合切换并保留只读协议/绑定/主机配置/链路状态 | 旧版在线切换曾写入不存在的 Android USB sysfs 节点并导致 gadget/ADB 消失；当前只确认 ADB 稳定和 RNDIS 无 carrier，ECM、DHCP、实际互联网转发仍未验证 |
| USB AUTO WAN | 带网线插入、获取地址、热点下游上网、拔线回退、修复后整卡拔出未重启 | AUTO接电脑不自动变LAN；原厂USB/PD驱动曾导致重启，不能保证已消除所有竞态 |
| Clash / Mihomo | B28：IPv4 TCP透明代理/DNS、国内直连与Google代理的开发机验证；B31：单台实机手动配置订阅、启用服务后确认代理可用，屏幕/网页叶子节点列表和网页选择回读通过 | 普通UDP、IPv6完整代理/泄漏、任意订阅格式、大规模订阅、干净安装后的自动启用及长期负载未验证 |
| 代理状态与覆盖自检 | 主机测试覆盖接管/直连/异常三种出口状态、部分规则缺失、IPv6 默认路由提示与自检脚本缺失；B31 实机回读确认 IPv4 TCP、DNS 和 UDP 443 拒绝规则，明确提示 IPv6 默认路由可能绕过 | 自检只反映本机转发规则，不检测客户端自带 DNS/DoH 绕过；普通 UDP、IPv6 全量代理与长期负载仍未验证 |
| 节点偏好与自动选择 | 主机测试覆盖收藏上限与去重、最近使用、离线节点过滤、偏好文件权限，以及 `url-test` 策略组的间隔/容差/按需检测参数和引用保护 | 自动选择的真实切换时序、蜂窝流量代价与手动固定后的行为仍需实机观察；不同订阅的节点命名未穷尽 |
| 统一代理结论 | 主机测试覆盖已核验接管、只配置未核验、部分转发和核心未运行四种结论，以及 GLOBAL 指向直连时拒绝全局代理 | 结论依赖只读转发规则回读；不检测客户端自带 DNS/DoH 绕过 |
| 订阅节点连通延迟 | B31 实机用 4 个订阅节点验证：由 provider 健康检查历史给出 675–3168 ms 的连通延迟，不再误报失败 | 该数值来自健康检查，不是按需即时测量；核心未开启健康检查或节点无历史时只提示“尚无检测记录”。不代表带宽测速 |
| 原地升级与回退 | B31 实机完成真实升级（`u60-pro-B31-20260923-155638` → `u60-pro-B31-20260924-072501`，随后 → `u60-pro-B31-20260925-001303`）：预检拒绝无安装、同版本、跨固件与错误设备；逐文件备份、哈希核对、网页与屏幕重启回读通过，代理配置、策略偏好、节点选择与订阅保持不变 | 只在已具备首次安装备份的设备上验证过；升级期间断电持久化边界仍未验证 |
| 资源占用（B31 实测） | 同一台 B31 上采样：亮屏空闲 Mihomo 平均 CPU 0.13%、RSS 32.3 MiB；切回原厂界面后增强屏幕进程释放（0 MiB）；经设备自身 `192.168.0.1:17890` 代理传输 40 MB／21 秒时 Mihomo 峰值 CPU 17.4%（单核口径）、RSS 37.2 MiB，可用内存全程保持约 752 MiB | 短时采样，不代表长时高负载或整夜功耗；空闲时 Tailscale 稳定占用约 10–13% 单核，若默认不需要组网应单独关闭；温度来自设备自报 CPU 温度，不是电池温度 |
| Tailscale | 本机组网、热点下属设备双向访问，网页访问实测，批准路由/Grant后通过 | 不自动批准管理端权限；跨所有ACL/出口节点/子网冲突组合未验证；首次登录新设备待验证 |
| 增强网页 | 原厂登录复用、共享动作、订阅与规则控制、充电同组按钮实机回读 | 新固件/页面结构变化拒绝加载；不是143个社区接口全部落地。当前节点、收藏/最近、自动选择与“版本与验证范围”由真实控制器数据与无头 DOM 渲染验证；分流确认、跨子页缓存、后台更新、登录切换、会话失效清理及跨页面实例请求串行通过页面模块回归。一级菜单对齐、单行标题、六个子页切换和分流确认弹窗已在一台 B31 的真实登录页面核对；代理状态与操作去重、当前节点前置、全组节点搜索、分批展开，以及规则/诊断入口和 USB/组网/电源分组已在桌面 Chrome 核对。后台重绘保留已展开节点数量、当前模式禁用和空连接状态纳入主机回归；手机窄屏与其他浏览器仍待实测 |
| 充电与外电输入 | 开发机停充/恢复、电流与外电在线、上限触发回读 | 新设备没有验收标记故默认锁定；完整充放电周期、长时温度、所有充电器/重启策略未实测；不是硬件电池隔离 |
| 深度待机 | 控制层/服务暂停恢复模拟、原厂休眠状态解析修复 | 最新修复后的无外设≥12分钟测试用户暂缓；整夜耗电改善尚未验证，无“6小时6%”承诺 |
| 首次安装/回退 | 独立目录模拟、错误固件拒绝、校验/已有文件拒绝、失败回滚测试 | 第二台实机首装、Windows端全流程、突然断电时持久化边界仍待验证 |

## 开机稳定性证据

B31 本次现场复现区分出两条独立链路：整机断电或 USB gadget 重建时，ADB transport 会正常消失，待原厂 ConfigFS 重新绑定 UDC 后恢复；旧版 USB 在线切换脚本还会写入不存在的 `/sys/class/android_usb/android0/enable`，可能把这次重建变成不可枚举状态。当前版本不再在线切换 USB 组合，开机只读确认 `ffs.adb`、UDC 和链路状态。

网页入口短暂消失的直接原因是旧 release 的 `u60-web` 服务未运行；设备上的网页文件和原厂指纹仍完整，但旧版增强脚本存在语法错误，导致入口恢复后页面不初始化。当前版本在 `scripts/test.sh` 中对全部网页脚本执行 `node --check`，并在 B31 启动钩子中延迟检查 `u60-web`，失败时最多重试三次并记录本地日志。当前 B31 安装已回读确认服务 running、`network-14b` 安装资源与生效挂载字节一致，控制器状态读取正常；升级器保留字节相同的网络核心 inode，旧升级遗留的运行核心只在路径与哈希一致时被接纳，B31 已确认恢复原代理接管。代理配置、订阅、节点偏好与 Tailscale 身份文件哈希保持一致。

## 安装风险

需要 root ADB，修改 `/data` 程序、`/etc/init.d` 与 `/etc/rc.local` 启动配置，可能造成失联、热点异常、启动失败、耗电、充电异常或需要人工恢复。原厂 OTA/恢复出厂可能覆盖启动项或使依赖不兼容；本项目不提供完整 EDL/Firehose 救砖方案，不提供固件分区备份。不要使用 opkg 强装不匹配的内核模块。

网页复用原厂 HTTP 管理通道；原厂管理密码和会话安全边界依旧存在。只在可信 LAN/私有 Tailscale 访问，不映射公网。订阅和中继密码必须由使用者自己输入；不要使用公开 issue 传递。

## 建议逐项验收

1. 不启用代理与组网，验证热点、原厂网页、屏幕和电源键。
2. 配置 Clash 后测试真实业务和规则命中，保留直连恢复入口。
3. Tailscale 先测本机，再测外部网络访问下属设备，管理端授权由本人完成。
4. USB 与中继分别测，不同时改变两个上游；观察下游真实联网和回退。
5. 最后测试休眠、按键唤醒与待机耗电；没有稳定基线前不启用深度待机。
6. 充电控制仅在独立硬件验收后启用；公开包不提供伪造验收开关。

## v0.1.3 新增验证范围

三主题持久化、非法值和保存失败处理；五菜单原生图标、点击范围、滚动边缘与长列表；模拟数小时休眠计时差、唤醒时 uptime 跳变、真实心跳停滞/缺失的看门狗保护均纳入回归。玻璃主题为静态效果，未完成整夜功耗对照。本次不扩大双频中继、充电、第二台设备首装等旧有验收边界。

开发机本轮已核对安装二进制哈希、海雾玻璃持久值、LCD 缓冲画面、原厂/新 UI 来回切换及持续心跳；Clash/Tailscale 进程、启动标识和其余设置保持不变。图标/点击区与三主题切换逻辑通过主机测试；用户对新版实体触摸手感和长待机结果仍可继续反馈。
