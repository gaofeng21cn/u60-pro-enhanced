## v0.1.14-experimental

- 把一次真实通过的原厂 ECM Mac 业务链路纳入 USB 状态诊断；普通入口仍保持关闭，避免把未通过恢复验收的切换发布给用户。
- 增加独立 ECM 维护事务：切换命令有界等待，超时和取消由监督进程通过原厂 USB composition owner 恢复，并回读 UDC、ADB FunctionFS 和配置状态。
- NCM 继续保持封锁，仅作为设备能力研究；TetherKit 仍是 RNDIS 的 Mac 备用路径。

# 更新记录

## v0.1.13-experimental · 2026-09-26

- 已保存上游在小屏与网页按实际重连优先级排列，首选和候选使用可理解的标签，不再显示内部权重数字。
- 重复将同一网络设为首选时不再重写凭据文件；当前连接保持不变。
- 小屏首页 USB 卡片使用简短链路状态，避免关键结论被截断；详细原因和上网验收状态仍在 USB 页面保留。
- 安装说明与 README 对齐，明确 Mac USB 上网与 TetherKit 材料尚未完成验收。
- macOS 原生 NCM 仍未通过实机恢复与业务验收，用户入口继续关闭。

## v0.1.12-experimental · 2026-09-26

- 新增最多 8 个已保存上游的管理、首选排序和断线重连；健康连接不抢切，密码只保存在设备。
- 新增一键网络自检，分别报告路由、地址、DNS、默认出口与 Clash 的 Google／Cloudflare HTTPS，严格保留证书校验。
- 补齐后台中继策略和网络管理动作白名单，统一小屏与网页入口。
- 修复升级失败时新增程序和服务未被完整移除的问题；缺失文件记录纳入备份校验，既有服务权限随备份恢复。
- Wi-Fi 中继显示实际连接阶段、蜂窝回退意图及 HTTP 检测结果时间，避免将连接上游或 HTTP 成功误报为完整联网。
- 修复旧默认 NTP 域名与代理 DNS 的循环依赖：新配置使用直接 IPv4 校时，旧配置提供 `--repair-ntp` 可回退修复；系统时间与自定义 NTP 保持不变。

## v0.1.11-experimental · 2026-09-26

- 明确 USB 数据线直连路线按“原生 NCM → TetherKit 降级”推进；原生能力未完成 Mac 真实业务验收前不开放普通用户开关。
- USB 诊断区分“内核存在 NCM”与“NCM 已通过切换验收”，待验证能力保持禁用；保留原厂 RNDIS 和 ADB 组合。
- 保留候选 NCM composition 源码；试运行和底层切换入口均已封锁，普通界面只显示待验证状态。旧 ECM 入口继续拒绝写入。

- 纠正超时回滚声明：同步切换阻塞不受原计时循环保护，现有测试不能证明实机恢复。原生 NCM 仍为开发主线，尚不可用。

## v0.1.10-experimental · 2026-09-25

- 增加 macOS RNDIS 用户态助手 `macos/u60-rndis.sh`：自动安装/检查 TetherKit、识别 U60 `19d2:1404`、启动 DHCP、查看状态和停止清理。
- Mac 直连路径保持 U60 原厂 RNDIS 和 ADB 组合，不再依赖 ECM 在线切换；默认不覆盖 Mac 现有默认路由，显式 `--route-all` 才切换出口。
- 更新安装、使用和验收文档，明确 TetherKit 是外部 MIT 组件，不随本项目重新分发。

## v0.1.9-experimental · 2026-09-25

- 补充 B31 ECM 限时试运行候选与主机回归；实机 USB 恢复未通过，入口保持禁用。
- 外接 USB 网卡按实际驱动和 USB 身份识别，LAN 支持常见 ASIX、Realtek、CDC Ethernet/NCM 与 Aquantia 驱动；AUTO 上游保留已验证 AX88179 限制。
- 更新 USB 接入说明与验证范围。


## v0.1.8-experimental · 2026-09-25

- 网页主导航和小屏统一“总览、网络、代理、组网”，Clash / Mihomo、Tailscale 保留为页面说明与技术入口。
- 总览减少重复入口，网络页区分连接与重连策略；统一卡片层级、间距、状态排版和响应式控件。
- 更新网页中过时的实机验证范围，保留当前真实限制。

## v0.1.7-experimental · 2026-09-25

- 优化 Tailscale 进程发现，先筛选候选再核对真实程序与运行参数；历史升级残留程序用逐字节比较替代重复哈希计算，保留重复进程拒绝和写入前身份核验。
- 小屏空闲时由触摸与后台结果唤醒，保留每秒实时采样；输入/选择弹窗期间暂停无关的完整状态读取。
- 不改变已保存的网络、代理和组网启用选择。

## v0.1.6-experimental · 2026-09-25

- 上游列表前置 2.4G / 5G 标记，显示信道和双频数量；解释 DFS、6GHz、企业认证等过滤原因，避免误以为仅支持 2.4GHz。
- 扫描、选网与连接共享频率限制，在输入密码前拒绝不兼容信道。
- 修正界面中过时的验证范围，区分 B31 已通过的 2.4GHz 中继与待验证的 5GHz 完整下游路径。

## v0.1.5-experimental · 2026-09-25

- 停止中继入口改为明确动作，并在总览标注“中继管理 / 停止”；Wi-Fi 页面优先显示中继动作、扫描与互联网状态。
- 新配置启用 Mihomo 内部 NTP，修复 B31 原厂本地时间 epoch 导致 VMess 握手失败；已有配置可显式运行 `setup-clash.sh --enable-ntp`，校验和热加载失败时回退，不修改原厂系统时间。
- B31 实机确认 Wi-Fi 上游关联、地址、出口、双热点及 Google/Cloudflare HTTPS；核心校时后同一代理节点请求通过。Mac 热点下游 IPv4 HTTPS 与透明代理链另经实测；冷启动、普通 UDP、IPv6 和长期稳定性仍未验证。

## v0.1.4-experimental · 2026-09-25

- 原地升级保留字节相同程序的 inode，修复升级后正在运行的代理被误判为失效；历史残留进程须通过原路径与完整字节哈希核对。
- USB 角色选择启动并核对协调服务；失败恢复原选择。B31 按用户启用意图恢复 USB 与 Wi-Fi 接力服务。
- 接力新增开机连接、允许/禁止蜂窝回退、忘记保存网络和绑定上游接口的互联网探测；更换上游保留蜂窝保护，探测失败不自动切换路由。
- 修复 B31 上游连接失败后热点无法恢复、扫描无响应：分固件协调信道，保留失败恢复记录；扫描去掉多余确认，错误持续显示。密码键盘提供独立字符页、至少 44 像素按键、8–63 位校验与失败后内存草稿重试。
- 小屏整理常用入口、管理子页、当前选项和确认内容；网页修复原厂菜单图标/对齐、会话缓存与请求串行，完善节点、组网和电源管理。
- 发布 B28/B31 设备绑定安装包，README 分开介绍下载、首装、原地升级、USB 网卡与数据线直连。未验收范围仍见 VALIDATION.md。

- 新增原地升级路径：`deploy-from-computer.py upgrade-check` 上传材料后确认固件、设备身份、已有安装和空间，`upgrade` 才写入。升级只替换程序与资源，备份每个被替换文件的哈希；替换后逐字节核对，网页与屏幕分别重启并回读，任一环节失败自动恢复上一版本程序。`upgrade-check` 通过不构成升级授权。
- 屏幕与网页新增「版本与验证范围」：显示已安装版本、核心版本、实际代理覆盖，以及已实机验证与仍未验证的项目，不再从运行中的程序推断版本或夸大范围。
- 新增只读资源采样工具 `scripts/measure-resources.py`，按场景记录进程 CPU、峰值 CPU、RSS、可用内存与设备自报温度，用于比较空闲与代理负载下的占用。
- 屏幕与网页共用一份“代理结论”：只有转发规则回读确认 IPv4 TCP 与 DNS 已生效才显示“已接管”；只配置未核验、转发不完整、核心未运行分别显示为不同状态，不再把“核心在运行”等同于“流量已被代理”。
- 全局代理在 GLOBAL 仍指向直连时被拒绝，并提示先选择可用节点；规则分流、全局代理与直连都通过控制层校验并回读。屏幕模式行会提前说明该前提。
- 新增设备本机保存的节点收藏与最近使用，屏幕和网页共享同一份偏好；收藏最多 20 个、最近使用 5 个，离线节点不会出现在快速入口。节点行新增连通延迟显示，统一把“测速”改称“连通延迟”，避免与带宽测速混淆。
- 增强网页默认进入“状态与节点”，可直接收藏、最近使用或从全部节点中切换；没有订阅时显示五步首次配置指引。连接视图使用更短的刷新间隔，其他视图保持原节奏。
- 新增可选的 `自动选择`（`url-test`）策略组：按连通延迟自动切换，初始 5 分钟间隔、50 ms 容差、按需检测；关闭或删除前会检查规则和其他策略组是否仍引用它。
- 修复订阅节点连通延迟始终失败：Mihomo 的 `/proxies/<名称>/delay` 只覆盖顶层代理，订阅节点不在其中。现在改为回退到该节点在 provider 中已有的健康检查记录；没有测量结果的节点明确提示“尚无检测记录”，不再显示成 0 ms 或笼统失败。
- 修复全局模式入口：GLOBAL 仍指向直连时拒绝切换到全局代理，并提供“全局模式入口”一步把 GLOBAL 指向真实策略组；链尾仍是直连时保持拒绝。修复前“全局代理”会显示成功而实际直连。
- 新增有界“最近操作”记录（最多 20 条，仅存动作、结果、时间与结果文案），屏幕可查看最近 10 条；不记录请求内容、订阅地址或凭据。
- Clash / Mihomo 代理状态拆成“核心在运行”和“流量已接管”两个事实，屏幕和网页都显示当前模式与 `MATCH / GLOBAL → 策略组 → 节点` 路径；核心停止时不再显示成已被接管。
- 新增只读“代理覆盖自检”：网络协调脚本回读实际 iptables 规则，报告 IPv4 TCP 与 DNS 是否生效、UDP 443 是否被拒绝、UDP 与 IPv6 未代理，并在存在 IPv6 默认路由时提示客户端可能绕过代理。自检只读取，不写状态、不占操作锁，失败时明确说明“无法核验”而不是假定成功。
- 订阅列表补充到期时间、用量缺失说明和“超过两个更新周期”提示；内置节点台页面补充核心、连接数与流量路径显示，节点列表支持搜索并对节点名做转义。

本版本提供首次安装和本项目既有安装的原地升级；不支持任意第三方布局迁移。USB 网卡下游、Mac 数据线直连、冷启动、长待机等未完成项按验证记录保留。

## v0.1.3-experimental · 2026-09-23

- 新增海雾玻璃主题，保留两套原配色；静态蓝灰渐变、半透明卡片、细边与浅色文字。RGB565 背景一次生成，不增加动画或唤醒定时器。
- 五个底部菜单补齐原生图标，图标和文字上下排列，保留原来的五等分点击区域。
- 修复长休眠后看门狗将正常 UI 当作卡死并退回原厂：不再比较 CLOCK_MONOTONIC 心跳和包含休眠的 /proc/uptime，改为观察心跳推进。真正停止推进仍触发重试及原厂回退。
- 新增三主题保存/错误处理、五图标/点击范围与模拟长休眠看门狗回归验证。

本轮仅修改屏幕展示及守护，不扩大网络、充电或待机的验证范围；不保证整夜耗电下降。安装包仍用于首次安装，不提供覆盖已有安装的自动升级。旧 Release 与恢复说明继续保留。

## v0.1.2-experimental · 2026-09-22

- 可保留自身 2.4G 与 5G 热点，选择一个 2.4G 或非 DFS 的 5G 上游；同名扫描结果按频段合并，同频段内重连，支持直接更换上游。
- 修复原厂驱动 CSA 未完成但 hostapd 报成功，导致手机认证失败。改用停用对应 BSS、运行时设置信道、重新启用，并同时核对驱动与 hostapd；切换会短暂中断该频段客户端。
- 修复关联尚未完成时反复重试，以及旧协调服务清理与新连接竞态；兼容 BusyBox flock，调整嵌套操作超时，保留失败恢复路径。
- 首页后台状态刷新保留本地实时出口/速率等字段，避免中继标题闪为运营商；实际蜂窝回退仍会更新。
- 屏幕菜单、节点列表和详情支持滑动，保留滚动位置、边缘裁剪，滑动不会误触选择。

开发机修复后的 2.4G 上游＋双热点均由用户确认百度/Google 正常；5G 上游有设备及 USB 下属自动测试，完整手机验收未完成。Tailscale 自动通信、业务出口流量计数及标题跨刷新画面检查通过。真实长休眠、冷启动、长期负载/功耗和第二台新机首装仍未验证。详见 [中继说明](WIFI-RELAY.md)。

本版提供对应源码标签、ARM64 安装包和 SHA-256 校验文件；旧版 Release 保留。安装包仍只支持首次安装，不能覆盖既有设备安装。没有通用无损升级命令，不要删除配置来绕过已有安装检查。

## v0.1.1-experimental · 2026-09-22

### 屏幕 UI

- 深色主题提高辅助文字对比度，并以蓝、青、紫区分网络、设备和服务卡片；浅色主题降低背景亮度并调整文字颜色。
- 设置项按可操作、只读状态、暂不可用分组；不改变操作参数或绕过验证门槛。
- 电池与省电整理为充电与直供、充电上限、USB-C 供电方向、待机服务策略、电池与待机详情。运行信息和未验证原因进入详情，异常状态仍突出显示。
- 修正已能操作的原厂设置中沿用的“状态不可读”等旧提示；SIM 页面明确为状态页。

### 原厂网页增强

- 移除与原厂重复的热点、蜂窝/APN、路由/DHCP/DNS/防火墙、短信、SIM 基础状态和套餐设置入口，原厂对应菜单继续使用。
- 保留完整 Clash/Tailscale 管理、Wi-Fi 中继、USB 网口角色、充电与深待机、频段/信号工具和代理连通性诊断。
- 分类菜单固定在页面底部，原厂侧栏的“增强功能”移至末尾；适配手机和桌面，避免原厂样式覆盖选中菜单。
- 屏幕独立套餐账本仍保留，未删除既有记录；不与原厂套餐账本自动同步。

### 验证与限制

开发机 B28 已验收屏幕启动与菜单、网页 375px/1280px 布局、原厂页面访问、订阅表单及组网高级状态。公共源码重新编译，新增菜单布局/高级提示/网页过滤和嵌套导航测试；公开版本仍不携带开发机配置或验收标记。

本轮是展示与提示更新，未扩大联网、射频、充电或待机的已验证范围。第二台新机首装、长待机耗电、充电完整周期、所有上游组合等限制见 [VALIDATION.md](VALIDATION.md)。

**安装包仍为首次安装流程，没有通用无损升级工具。** 已有 v0.1.0 或其他改装的设备不能直接执行首装覆盖；先保留原配置和备份，按源码差异制定单独更新方案。不要删除项目目录、订阅或 Tailscale 身份来绕过安装保护。

## v0.1.0-experimental

首个国行 B28 公开实验版，提供屏幕与原厂网页增强、首次安装及回退材料。
